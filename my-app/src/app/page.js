"use client";
import React from "react";
import styled from "styled-components";

const Root = styled.div``;

const Home = async () => {
  return (
    <Root>
      <h1>home</h1>
    </Root>
  );
};

export default Home;
